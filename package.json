{
  "name": "ai-image-editor-root",
  "version": "1.0.0",
  "description": "AI Image Editor Monorepo",
  "private": true,
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "npm run start:dev --workspace=backend",
    "dev:frontend": "npm run dev --workspace=frontend",
    "install:all": "npm install"
  },
  "devDependencies": {
    "concurrently": "^8.0.0"
  },
  "dependencies": {
    "@vitejs/plugin-vue": "^5.2.4"
  }
}
